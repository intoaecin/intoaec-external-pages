import PaymentFailed from "@/features/components/subscription/PaymentFailed";
import React from "react";

const Home = () => {
  return (
    <div>
      <PaymentFailed />
    </div>
  );
};

export default Home;
